# Deep Clean Baseline (2026-06-03)

This file captures the pre-cleanup baseline for the LockSafe repository.

## Scope

- Program: Deep Clean Program (aggressive, archive-first)
- Wave: Phase 0 baseline + Phase 1 docs/config drift fixes
- Branch: feat/self-learning-playbook

## Baseline Metrics

- root_command_files: 45
- scripts_ts_files: 117
- docs_markdown_files: 47
- cron_routes: 46
- api_route_files: 335
- npm_scripts_count: 38

## Observed Drift / Risks

1. Script config drift:
   - Both `tsconfig.scripts.json` and `scripts/tsconfig.scripts.json` are used.
2. Package script drift:
   - Duplicate `postinstall` key existed in `package.json` scripts object.
3. Cron docs drift:
   - `docs/CRON_SETUP.md` was long-form and partially stale relative to current auth behavior in `src/lib/cron-auth.ts`.

## Guardrails for Cleanup

1. Archive-first for uncertain removals.
2. Delete only with evidence:
   - no references found
   - no runtime dependency
3. Verify after each wave:
   - lint/build
   - targeted operational smoke checks

## Wave 1 changes (started)

- Normalized `test:system*` scripts to use root `tsconfig.scripts.json`.
- Removed duplicate `postinstall` script key from `package.json`.
- Rewrote `docs/CRON_SETUP.md` to reflect current endpoint and auth model.
